apiVersion: apps/v1
kind: Deployment
metadata:
  name: housekeep
  labels:
    app: housekeep
    appCode: load-generator
spec:
  replicas: {{ .Values.housekeep.replicas }}
  selector:
    matchLabels:
      app: housekeep
      appCode: load-generator
  template:
    metadata:
      labels:
        app: housekeep
        appCode: load-generator
    spec:
      containers:
      - name: housekeep
        image: {{ .Values.global.image.repository }}:{{ .Values.housekeep.image.tag }}
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
            name: loadgen-config
        - secretRef:
            name: loadgen-secret
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        volumeMounts:
        - name: load-gen-data
          mountPath: /data/load_gen
      volumes:
      - name: load-gen-data
        persistentVolumeClaim:
          claimName: load-gen-pvc