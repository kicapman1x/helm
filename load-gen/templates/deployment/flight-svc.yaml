apiVersion: apps/v1
kind: Deployment
metadata:
  name: flight-svc
  labels:
    app: flight-svc
    appCode: load-generator
spec:
  replicas: {{ .Values.flight-svc.replicas }}
  selector:
    matchLabels:
      app: flight-svc
      appCode: load-generator
  template:
    metadata:
      labels:
        app: flight-svc
        appCode: load-generator
    spec:
      containers:
      - name: flight-svc
        image: {{ .Values.global.image.repository }}:{{ .Values.flight-svc.image.tag }}
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
            name: loadgen-config
        - secretRef:
            name: loadgen-secret
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        volumeMounts:
        - name: load-gen-data
          mountPath: /data/load_gen
      volumes:
      - name: load-gen-data
        persistentVolumeClaim:
          claimName: load-gen-pvc