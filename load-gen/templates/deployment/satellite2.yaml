apiVersion: apps/v1
kind: Deployment
metadata:
  name: satellite2
  labels:
    app: satellite2
    appCode: load-generator
spec:
  replicas: {{ .Values.satellite2.replicas }}
  selector:
    matchLabels:
      app: satellite2
      appCode: load-generator
  template:
    metadata:
      labels:
        app: satellite2
        appCode: load-generator
    spec:
      containers:
      - name: satellite2
        image: {{ .Values.global.image.repository }}:{{ .Values.satellite2.image.tag }}
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
            name: loadgen-config
        - secretRef:
            name: loadgen-secret
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        volumeMounts:
        - name: load-gen-data
          mountPath: /data/load_gen
      volumes:
      - name: load-gen-data
        persistentVolumeClaim:
          claimName: load-gen-pvc