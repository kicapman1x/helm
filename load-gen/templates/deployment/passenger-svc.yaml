apiVersion: apps/v1
kind: Deployment
metadata:
  name: passenger-svc
  labels:
    app: passenger-svc
    appCode: load-generator
spec:
  replicas: {{ .Values.passenger-svc.replicas }}
  selector:
    matchLabels:
      app: passenger-svc
      appCode: load-generator
  template:
    metadata:
      labels:
        app: passenger-svc
        appCode: load-generator
    spec:
      containers:
      - name: passenger-svc
        image: {{ .Values.global.image.repository }}:{{ .Values.passenger-svc.image.tag }}
        imagePullPolicy: Always
        envFrom:
        - configMapRef:
            name: loadgen-config
        - secretRef:
            name: loadgen-secret
        env:
        - name: PYTHONUNBUFFERED
          value: "1"
        volumeMounts:
        - name: load-gen-data
          mountPath: /data/load_gen
      volumes:
      - name: load-gen-data
        persistentVolumeClaim:
          claimName: load-gen-pvc